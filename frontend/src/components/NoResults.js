/**
 * Displays an "No result !" text.
 *
 * @returns {JSX.Element}
 *
 */
const NoResults = () => {
    return (
        <div className="w-max mx-auto">
            <strong className="text-lg">Aucun résultat !</strong>
        </div>
    )
}

export default NoResults;